* Make into bare commandline tool
* Print out results at the end of execution
* Mapping perf events to/from strings (and so, custom events)
* Define measuring configuration format
* Use ninja build system?
* Testing as much as possible
